# v015 8x8 글꼴 전용 치환 후보 — 폐기

> 사용자 실행 비교에서 `스탠다드 → 시나리오 로드 → 실행` 전환이
> v015에서는 비정상적으로 오래 지연되고, 동일한 시나리오/레코드를 가진
> v013에서는 즉시 정상 실행되는 것이 확인되었다. 따라서 단순 비트맵으로
> 판단한 `0x3850E..0x38D0D` 범위에도 실행 시 의미가 있는 공유 데이터가
> 포함되었을 가능성을 배제할 수 없다. 이 ROM은 부모·배포·검증 기준으로
> 사용하지 않는다.

- v014는 폐기했다. `0x3850E+0x800` 이후의 전투 표시용 DMA 타일을
  글꼴로 오인해 변경한 것이 원인이었다.
- v015는 일본판 Rev. A 원본에서 다시 빌드했다.
- 16x16 치환은 v013과 동일하다.
- 8x8 치환은 실제 글꼴 범위 `0x3850E..0x38D0D` 안에서만 수행했다.
- DMA 꼬리 `0x38D0E..0x38F9D`는 일본판 원본과 완전 동일하다.
- 출력: `out/fixed-record-kana-kanji-8x8-v015.md`
- 크기: 1,048,576 bytes
- SHA-256: `948F0091817C46E8B8A98CBAE794DBA8DF7B84305AB9C7700F3889DBB8A3D0AF`
- 메가드라이브 체크섬: `0x86EC`
- 8x8 치환 글리프: 169개
- 변경된 레코드: 0
- 허용 범위 밖 변경: 0
- UI 레코드 영역 `0xEF000..0xEFFFF`: v013과 완전 동일
- 사용자 실행 검증 실패: 폐기.
- 복귀 기준: `out/fixed-record-kana-kanji-phonetic-v013.md`
- 재시도 조건: 8x8 각 슬롯의 DMA/읽기 참조를 실행 추적으로 입증한 뒤,
  순수 글리프임이 확인된 슬롯만 개별 적용한다.
