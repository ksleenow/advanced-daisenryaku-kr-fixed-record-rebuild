# v025 8x8 가타카나 `카` 단일 확장 체크포인트

## 변경

- 부모: v023 8x8 가타카나 모음 체크포인트
- 출력: `out/fixed-record-8x8-katakana-ka-v025.md`
- 원본 8x8 시트와 16x16 대응표가 모두 `0x4E = カ`임을 확인한 뒤
  해당 슬롯의 비트맵만 `카`로 교체했다.
- 유지 슬롯: `0x49..0x4D = 아이우에오`
- 숫자 슬롯 `0x00..0x09`는 변경 금지 영역으로 유지했다.

## 정적 검증

- SHA-256: `426AA1DEF9CBA8352CEDF9052C1C7A10435AFD4B995CB1C042DED1E7924CBD90`
- 크기: 1,048,576바이트
- 메가드라이브 체크섬: `0x4458`
- v023 대비 차이: 체크섬 2바이트와 `0x4E` 슬롯 내부 7바이트뿐
- 숫자 `0x00..0x09`: 일본판 원본과 완전 동일
- 16x16 글꼴 및 `0xEF000..0xEFFFF` 레코드: v023과 완전 동일
- 문자열, 포인터, 코드, VRAM 배치 변경: 0

## 실행 검증 범위

표준 시나리오 목록에서 숫자 `0..9`가 유지되는지 먼저 확인한다. 이어서
최초 표시, 포커스 진입/이탈, 재진입 및 지도 복귀에서 새 `카` 글리프와
기존 화면에 회귀가 없는지 확인한다.
